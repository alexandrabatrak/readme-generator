{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "CLI app to generate README.md for GitHub repositories with inquirer.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexandra Batrak",
  "license": "MIT",
  "dependencies": {
    "inquirer": "^8.0.0"
  }
}
